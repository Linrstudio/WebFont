#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ttf2woff/ttf2woff.js" "$@"
  ret=$?
else 
  node  "$basedir/../ttf2woff/ttf2woff.js" "$@"
  ret=$?
fi
exit $ret
