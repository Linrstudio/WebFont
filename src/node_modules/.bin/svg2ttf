#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../svg2ttf/svg2ttf.js" "$@"
  ret=$?
else 
  node  "$basedir/../svg2ttf/svg2ttf.js" "$@"
  ret=$?
fi
exit $ret
